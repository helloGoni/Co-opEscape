using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class PlayerMover : MonoBehaviour
{
    /*
    Dictionary<KeyCode, Action> keyDict;

    void Start()
    {
        keyDict = new Dictionary<KeyCode, Action> {
            { KeyCode.A, KeyDown_A },
            { KeyCode.B, KeyDown_B }
        };
    }

    void Update()
    {
        if(Input.anyKeyDown) {
            foreach (var dic in keyDict) {
                if(Input.GetKeyDown(dic.Key)) {
                    dic.Value();
                }
            }
        }
    }
    #region KeyAction

    private void KeyDown_A() {
        Debug.Log("A");
    }

    private void KeyDown_B() {
        Debug.Log("B");
    }

    #endregion*/
}

//https://www.youtube.com/watch?v=ODB3IOFqmrE 플레잉어 키 바꾸기